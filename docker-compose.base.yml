services:
  steve:
    build: .
    container_name: steve
    restart: unless-stopped
    stdin_open: true
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - steve-data:/data
      - steve-vault:/vault
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - STEVE_HOST_IP=${STEVE_HOST_IP:-localhost}
      - STEVE_VAULT_PASSWORD=${STEVE_VAULT_PASSWORD:-}
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    networks: [steve-net]

volumes:
  steve-data:
  steve-vault:
  opencode-auth:

networks:
  steve-net:
    driver: bridge
